{
  "name": "ad-preview-capture",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for Ad Preview Capture – load AdUnit/Craftsman+ URLs, interact, and record to .MOV",
  "workspaces": [
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --workspace=packages/backend\" \"npm run dev --workspace=packages/frontend\"",
    "build": "npm run build --workspace=packages/backend && npm run build --workspace=packages/frontend",
    "lint": "npm run lint --workspace=packages/backend && npm run lint --workspace=packages/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
